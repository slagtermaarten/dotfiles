#!/bin/zsh

TMPDIR=$(mktemp -d /tmp/git-latexdiff.XXXXXX)
latexdiff -t CTRADITIONAL "$1" "$2" > $TMPDIR/diff.tex
pdflatex -interaction nonstopmode -output-directory $TMPDIR $TMPDIR/diff.tex
evince $TMPDIR/diff.pdf
rm -rf $TMPDIR
